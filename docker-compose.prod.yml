version: "3.9"

services:
  app:
    build: 
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env.production
    # No volumes in production for security and performance